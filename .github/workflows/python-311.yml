name: python-311

on:
  push:
    branches:
      - master
  schedule:
    - cron: "11 * * * *"

jobs:
  sync:
    uses: ./.github/workflows/sync.yml
    with:
      version: "3.11"
    secrets: inherit
    
