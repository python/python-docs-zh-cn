name: python-38

on: workflow_dispatch

permissions:
  contents: read

jobs:
  sync:
    uses: ./.github/workflows/sync.yml
    with:
      version: "3.8"
      tx_project: "python-38"
    secrets: inherit