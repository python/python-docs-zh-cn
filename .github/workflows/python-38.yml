name: python-38

on:
  push:
    branches:
      - master
  schedule:
    - cron: "38 * * * *"

jobs:
  sync:
    uses: ./.github/workflows/sync.yml
    with:
      version: "3.8"
    secrets: inherit