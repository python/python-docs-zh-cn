name: python-39

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync:
    uses: ./.github/workflows/sync.yml
    with:
      version: "3.9"
      tx_project: "python-39"
    secrets: inherit
    